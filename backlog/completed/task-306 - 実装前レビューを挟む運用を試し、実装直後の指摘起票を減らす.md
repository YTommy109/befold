---
id: TASK-306
title: 実装前レビューを挟む運用を試し、実装直後の指摘起票を減らす
status: Done
assignee:
  - '@claude'
created_date: '2026-08-04 17:05'
updated_date: '2026-08-05 13:55'
labels: []
dependencies: []
priority: medium
ordinal: 492000
---

## Description

<!-- SECTION:DESCRIPTION:BEGIN -->
コミット実測（1,176 コミット）で fix 254 件が feat 186 件を上回っている。レビュー→起票→修正のループが回っている健全さの表れでもあるが、実装完了直後に同じ機能で複数件がまとめて起票されるケースが目立つ（例: コミット docs: code-review で確認された絞り込みの不具合を 7 件起票する、chore: レビュー指摘を TASK-278〜281 として起票する、chore: コードレビュー指摘を TASK-270〜274 として起票する）。

実装後にしか品質ゲートがないため、同一機能内の設計不整合が実装を一通り終えてから発見され、手戻りになっている。実装着手前に差分の設計だけをレビューする段を挟めば、同じ指摘をより安く回収できるか検証する。
<!-- SECTION:DESCRIPTION:END -->

## Acceptance Criteria
<!-- AC:BEGIN -->
- [x] #1 直近の「実装直後に複数件起票」となった機能を 3 例選び、各指摘が実装前レビューで検出可能だったかを実際の指摘内容に照らして分類した結果が Notes に記録されている
- [x] #2 実装前レビューで検出可能だった割合が有意（過半）であれば、着手前レビューの段を運用（スキルまたは CLAUDE.md）へ組み込む
- [ ] #3 有意でなければ「実施しない」と結論と根拠を Notes に記録し、運用は変更しない
<!-- AC:END -->

## Implementation Notes

<!-- SECTION:NOTES:BEGIN -->
## AC#1 分類（3 例・計 16 タスク）

対象コミット: 2f64b596（TASK-285〜291 / 変更ファイル絞り込み TASK-264 の直後）、b50542b3（TASK-278〜281 / TASK-273・274 の直後）、94030a43（TASK-270〜274 / TASK-266・268 の直後）。

判定基準（厳しめ）: **実装コードを読まず・実測せず、着手前の設計文（何をどこに置くか）だけから導けるか**。導けるものを「事前検出可」とした。

### 事前検出可（12 件）

- TASK-285 絞り込み判定の真実の源（未追跡ディレクトリの畳み込み / 綺麗なリポジトリでの誤縮退 / 状態の遅延到着）: 「gitStatuses にキーがあるか」でメンバーシップと縮退を決める、という設計文そのものから導ける
- TASK-286 ensureCurrentFile の不変条件を visibleEntries 側の絞り込みが迂回: 既存不変条件と新設述語の置き場所の衝突で、純粋に設計レベル
- TASK-287 絞り込みで空になったときの文言: 新しい空状態を作るなら表示を決める必要がある、という設計上の欠落
- TASK-288 フォルダープレビューに設定が渡っていない: 新設の表示設定の消費経路の列挙漏れ。既存 spec の契約とも矛盾
- TASK-291 トグルで refreshAllSidebars を再利用: 「既存の再読み込み経路を再利用する」設計と、純粋な表示述語という性質の不一致
- TASK-278 previewTarget キャッシュの陳腐化: 「computed を didSet キャッシュへ変える」設計に対し『解決入力は純粋か』を問えば出る
- TASK-279 native backing 変換の張り直しが 3 経路目で漏れ: 「境界の変換を外し消費側で張り直す」設計に対する消費経路の全列挙漏れ
- TASK-280 プローブがゲート対象と別 API を測る（他 3 件）: 「プローブでアサートをゲートする」設計に対し『測る対象とアサート対象は同じか』『skip は記録されるか』『本番 API を呼ぶか』はすべて設計時の問い
- TASK-270 初回ファイルで保存倍率が復元されない: 「WebView を常駐させる」設計が初期化順序を変える、という帰結（既存の順序依存処理の棚卸しが要る）
- TASK-271 不可視文書に操作・フォーカス・VoiceOver が届く: 「opacity 0 で常駐」の帰結を全入口で列挙していれば出る（実装は 3 経路だけ塞いだ）
- TASK-272 不可視 WebView が再描画され続ける: 同上、常駐化の帰結
- TASK-273 メニュー検証ごとの全件走査 / 列挙側で捨てる URL 再構築: 『高頻度経路に線形走査を置くのか』『正規化を境界に置くと全消費者が払う』という設計時の問い

### 事前検出不可（3 件）＋部分 1 件

- TASK-289 FeatureGate 参照の列挙ずれ: 実装した既定引数を grep して初めて分かる同期漏れ
- TASK-290 テストのフィクスチャ・永続化テストの前提: 書かれたテストコードそのものの問題
- TASK-281 allEntriesSorted の二重ソート: TASK-273 以前からある既存コードの無駄。差分の設計からは出ない
- TASK-274 は部分（#1 の macOS 14 依存アサートは『全対応 OS で成り立つ前提か』として設計時に問える／#2 の weak への一時オブジェクト代入はテスト実装の詳細）

### 結論

12〜12.5 / 16 = **約 75〜78%** が事前検出可で、過半を明確に超える。よって AC#2 に従い、着手前レビューの段を運用へ組み込む。

検出不可だったものには共通の型があり、いずれも『既存コードの無駄』『テストコードの書き方』『実装後の grep でしか分からない同期漏れ』で、実装後レビュー（/code-review）の担当範囲として残す。

## AC#2 の運用組み込み

過半（約 75〜78%）だったため、着手前レビューの段を組み込んだ。

- `.claude/skills/review-design.md` を新設。分類で拾えた 12 件がどの問いで検出できたかを 7 項目のチェックリストに落とした（判定の真実の源 / 既存の不変条件との衝突 / 消費経路の全列挙 / 新しい状態に対応する表示 / ライフサイクル・順序の変化 / 高頻度経路のコスト / 測るものと守るものの一致）。検出できなかった 3 型は「実装後レビューに任せる」と明記した
- `.claude/commands/review-design.md`（`/review-design`）を新設
- `.claude/CLAUDE.md` に「実装着手前の設計レビュー」節を追加し、適用対象（状態・述語・表示設定の追加、値の持ち方の変更、既存の不変条件・共通経路への変更）と非対象（局所修正）、根拠の実測値を書いた

markdownlint-cli2: 64 ファイル 0 件で通過。

AC#3 は「有意でなければ」の条件付きのため対象外（未チェックのまま残す）。
<!-- SECTION:NOTES:END -->

## Final Summary

<!-- SECTION:FINAL_SUMMARY:BEGIN -->
実装直後にまとめて起票された 3 例・16 件のレビュー指摘を、『実装コードを読まず設計文だけから導けるか』の基準で分類した。12〜12.5 件（約 75〜78%）が事前検出可で過半を明確に超えたため、AC#2 に従い着手前レビューを運用へ組み込んだ。review-design スキルと /review-design コマンドを新設し、検出できた 12 件を 7 項目のチェックリストに落とし、検出できなかった 3 型（既存コードの無駄・テストコードの書き方・実装後の grep でしか分からない同期漏れ）は実装後レビューの担当として明記した。.claude/CLAUDE.md に適用条件と実測根拠を記載。markdownlint-cli2 は 0 件。
<!-- SECTION:FINAL_SUMMARY:END -->
