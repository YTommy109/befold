---
id: TASK-431
title: 400 行を超えたテストファイル 4 件をスイート単位で分割する
status: Done
assignee:
  - '@claude'
created_date: '2026-08-10 12:35'
updated_date: '2026-08-12 02:15'
labels: []
dependencies:
  - TASK-428.1
priority: medium
type: chore
ordinal: 100900
---

## Description

<!-- SECTION:DESCRIPTION:BEGIN -->
`BefoldApp/.swiftlint.yml` の `excluded` は `.build` と `befold/Resources` のみで、テストターゲットも `file_length` の対象になっている。2026-08-10 時点で 400 行を超えているテストは次の 4 件（`wc -l` 実測）。

- `BefoldApp/befoldTests/ViewerWindowControllerTests.swift` 585
- `BefoldApp/befoldTests/ViewerStoreTests.swift` 540
- `BefoldApp/befoldTests/GitCommandRunnerTests.swift` 507
- `BefoldApp/befoldTests/QuickOpenModelTests.swift` 452

TASK-428 のラチェットを最終的に撤去して単純な閾値強制へ畳むには、この返済が必要。分割の前例は `DirectoryListerAppendingOpenFileTests`（同じ理由でスイートを分けたもの）。判断基準は `docs/dev/rules/testing.md` に従い、検証対象の凝集単位でスイートを分けること（行数を均すためだけの機械的な半分割にしない）。

依存: TASK-428.1 が型グループの集計単位とテストの扱いを確定させるため、その決定を確認してから着手するのが安全（テストを対象範囲に含めない決定になれば、このタスクは不要になる可能性がある）。
<!-- SECTION:DESCRIPTION:END -->

## Acceptance Criteria
<!-- AC:BEGIN -->
- [x] #1 対象 4 ファイルがいずれも 400 行以下になる
- [x] #2 分割がスイートの凝集単位で行われている（各スイートが何を検証するかを 1 行で言える）
- [x] #3 テストの総数が分割前後で変わらない（欠落していない）
- [x] #4 swift test が既存どおり通る
- [x] #5 xcodegen generate 済みで xcodebuild build が通る
<!-- AC:END -->

## Implementation Plan

<!-- SECTION:PLAN:BEGIN -->
1. 対象 3 ファイル (ViewerWindowControllerTests 585 / ViewerStoreTests 540 / QuickOpenModelTests 452) を凝集単位で独立スイートへ分割する（DirectoryLister 系の前例に倣い Foo+Bar.swift の extension 方式は採らない。型グループ集計が合算するため）
2. 共有ヘルパーの重複コピーを避け、必要なものだけ internal へ上げる
3. xcodegen generate → swift build / swift test で検証し、@Test 件数が分割前後で不変であることを確認する
4. scripts/check-type-group-size.sh でテストグループが閾値以下になったことを確認し、type-group-baseline.txt から該当エントリを削除する
<!-- SECTION:PLAN:END -->

## Implementation Notes

<!-- SECTION:NOTES:BEGIN -->
起票時に対象としていた GitCommandRunnerTests.swift（507 行）は現在リポジトリに存在しない（find で 0 件）。型グループ単位のベースライン（scripts/type-group-baseline.txt）に残るテストグループは ViewerWindowControllerTests 585 / ViewerStoreTests 540 / QuickOpenModelTests 452 の 3 件。対象は 4 件ではなく 3 件。

2026-08-11 時点の実測（scripts/check-type-group-size.sh）: ViewerWindowControllerTests は TASK-449 のテスト追加で 585 → 629 行になった。残る 3 件は ViewerWindowControllerTests 629 / ViewerStoreTests 540 / QuickOpenModelTests 452。

## 実施内容（2026-08-12）

対象 3 グループを、行数を均す機械的分割ではなくスイートの凝集単位で独立型へ分割した。分割方式は `Foo+Bar.swift` の extension ではなく `DirectoryLister*Tests` に倣った独立 `@Suite struct`。extension 方式では check-type-group-size.sh が合算するため負債返済にならない。

### ViewerWindowControllerTests 585 → 5 スイート（最大 146 行）
- ViewerWindowControllerTests 125: 新規ウィンドウの初期状態と WindowFrameStore へのフレーム記録
- ViewerWindowControllerFileSwitchTests 105: switchFile / handleRename の副作用
- ViewerWindowControllerHistoryTests 99: 戻る/進む履歴とメニュー有効判定
- ViewerWindowControllerRefResolutionTests 146: 参照解決そのもの
- ViewerWindowControllerReferenceOpenTests 98: 解決済み参照を開く経路
- ViewerWindowControllerTestSupport 81: 共有スタブ・ヘルパー（テスト本体なし）

### ViewerStoreTests 540 → 4 スイート（最大 149 行）
- ViewerStoreTests 149: 開く／切り替えの基本フローと表示設定
- ViewerStoreUnsupportedFileTests 106: 非対応ファイルの reject 判定と解除
- ViewerStoreBinaryContentTests 93: 画像・PDF の base64 読み込み
- ViewerStoreWatcherCallbackTests 148: 監視コールバックによる再読込
- ViewerStoreTestSupport 84: 共有モック・ヘルパー（他 6 スイートからも参照）

### QuickOpenModelTests 452 → 5 スイート（最大 125 行、元ファイルは削除）
- QuickOpenModelCandidateSetArrivalTests 67 / FuzzyFilterTests 65 / PathModeTests 87 / TabCompletionTests 64 / SelectionCommitTests 125 / TestSupport 63

### 決定・副次的な整理
- 共有ヘルパーは Swift の private がファイルスコープであるため `*TestSupport.swift` へ internal で集約した（既存の DirectoryListingTestSupport / DiffTestSupport と同じ形）。重複コピーはしていない。
- QuickOpen が持っていた private `actor AsyncGate` は削除し、共有ヘルパー BefoldTestSupport/AsyncGate へ寄せた（テスト規約「共有ヘルパーは BefoldTestSupport で満たす」に沿う）。
- ViewerWindowControllerReferenceResolutionTests（46 字）は swiftlint の type_name（上限 40 字）に新規で触れたため ViewerWindowControllerRefResolutionTests（39 字）へ改名した。

### 検証（実測）
- swift test: 1442 tests / 225 suites すべて通過。
- @Test 件数: 分割前 20 + 22 + 24 = 66、分割後も 66（`git show HEAD:<file> | grep -c '@Test'` と分割後ファイル群の合計で照合）。
- xcodegen generate 済み、xcodebuild build -scheme befold: BUILD SUCCEEDED。
- scripts/check-type-group-size.sh --check: exit 0（"型グループの行数はベースライン以内です"）。--over の残りは ViewerWindowController 802 / ViewerWindowManager 610 / ViewerStore 531 のプロダクト 3 件のみで、テストグループは全滅。
- scripts/type-group-baseline.txt を --update-baseline で締め直し、テスト 3 グループのエントリを削除（3 行削除）。
- swiftlint: origin/main ベースライン 61 件 → 56 件。差分は削除 5 件と、file_length/type_body_length の消滅・既存 opening_brace と large_tuple の移動のみで、新規ルール違反ゼロ。
- swiftformat lint: 0 件。
<!-- SECTION:NOTES:END -->

## Final Summary

<!-- SECTION:FINAL_SUMMARY:BEGIN -->
400 行を超えていたテスト 3 グループ（ViewerWindowControllerTests 585 / ViewerStoreTests 540 / QuickOpenModelTests 452）を、スイートの凝集単位で 14 の独立 @Suite + 3 つの *TestSupport へ分割し、全ファイルを 400 行以下（最大 149 行）にした。テスト件数は 66 のまま不変で、swift test 1442 件全通過・xcodebuild BUILD SUCCEEDED・swiftlint はベースライン差分ゼロ（61→56）。type-group-baseline.txt からテスト 3 グループのエントリが消え、TASK-428.5 のラチェット撤去に必要な返済のうちテスト側が完了した。
<!-- SECTION:FINAL_SUMMARY:END -->
