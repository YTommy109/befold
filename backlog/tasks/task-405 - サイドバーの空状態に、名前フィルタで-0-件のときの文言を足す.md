---
id: TASK-405
title: サイドバーの空状態に、名前フィルタで 0 件のときの文言を足す
status: To Do
assignee: []
created_date: '2026-08-10 04:00'
labels: []
dependencies: []
priority: medium
type: bug
ordinal: 662000
---

## Description

<!-- SECTION:DESCRIPTION:BEGIN -->
`SidebarEmptyState`（Viewer/SidebarEmptyState.swift）は **git 絞り込みか否かの 2 分岐しか持たない**。名前フィルタ（filterText）で 0 件になったときは「対応ファイルがありません」側に落ちるため、開ける文書で満たされたフォルダーでも読み込みに失敗したように読める。

これは TASK-287 が git 絞り込みについて直したのと同型の問題が、名前フィルタ側に残っているもの。

## いつ目立つか

ツリー展開（TASK-361.4）でサブフォルダを開いている状態だと、「開いているのに何も無い」ように見えて食い違いがより目立つ。

## 現状

TASK-361.5（ツリー展開時のフィルタの意味）のレビューで発見した既存の欠落。祖先保持の実装はブロックしないため、そちらでは扱わなかった。

## 参考

- `Viewer/SidebarEmptyState.swift` — 出し分けの実装（サイドバーとプレビューで共有）
- `Viewer/FileListView.swift` / `Viewer/FolderListingView.swift` — 呼び出し元 2 箇所
- 文言キーは `sidebar.empty` / `sidebar.empty.changedFilesOnly` / `sidebar.empty.changedFilesOnly.description`
<!-- SECTION:DESCRIPTION:END -->

## Acceptance Criteria
<!-- AC:BEGIN -->
- [ ] #1 名前フィルタで 0 件になったとき、絞り込みが効いていることが分かる文言が出る
- [ ] #2 git 絞り込み・名前フィルタ・その両方、の区別が出し分けられている
- [ ] #3 サイドバーとプレビュー内フォルダー一覧の両方に同じ出し分けが効く（SidebarEmptyState を共有したまま）
- [ ] #4 出し分けの判定が純粋関数として切り出され、ユニットテストで担保されている
<!-- AC:END -->
