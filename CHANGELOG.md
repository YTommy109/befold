# Changelog

stable リリースのユーザー影響のある変更を記録する。
dev リリースの全変更履歴は [GitHub Releases](https://github.com/YTommy109/befold/releases) を参照。
v1.5.0 より前の履歴は GitHub Releases の各タグページを参照。

## v1.8.0

### 新機能
- QuickLook 拡張を追加しました。Finder でファイルを選んでスペースキーを押すだけで、befold を起動せずに Mermaid / Markdown のプレビューを表示できます。対象拡張子は `.mmd` / `.mermaid` / `.md` / `.markdown` などで、プレビューは描画完了を待ってから表示されるため途中の状態が見えることはありません。
- 大きいファイルや行指向でないファイルでも、QuickLook のプレビューが空白のまま止まらないようにしました。

## v1.7.3

### 新機能
- ドキュメント内のパス参照を表示時に解決し、実在するファイルだけをリンクとして表示するようになりました。相対パスで見つからない場合は git 追跡ファイルへフォールバックし、サフィックス一致と近さでもっとも妥当な候補を選びます。

### 修正
- パス参照のリンク化に関する不具合を修正しました（エスケープされた `#` をファイル名の一部として扱う、存在しないファイルをリンク化してしまうケースの排除、git 追跡ファイル一致時の実在確認）。
- git の一時的な失敗が解決結果としてキャッシュに固定されないようにしました。
- git 実行をリポジトリ設定から切り離し（絶対パス・固定環境）、設定由来で任意のコマンドが実行されうる問題を塞ぎました。
- git 実行を打ち切った際にプロセスやスレッド、ファイルディスクリプタが残る問題を修正しました。
- CLI から追加したブックマークがツールバーへ即座に反映されるようにしました。
- CLI と GUI で同時にブックマークを更新した際に書き込みが失われる問題を修正しました。
- CLI の転送完了待ちを非同期化し、転送の成否を正しく判定するようにしました。
- CLI からのアプリ起動に失敗した場合、stderr に診断メッセージを出力するようにしました。

### パフォーマンス
- パス参照の解決をメインスレッド外で実行し、候補索引をバッチ全体で使い回すようにして、表示時の待ち時間を短縮しました。
- git 追跡ファイルの照合索引をキャッシュし、バッチごとの再構築をなくしました。

## v1.7.2

### 修正
- HTML 表示直後の切替で空白表示になる問題を修正
- 完全一致・末尾改行なしのファイルで偽のトランケーション表示が出る問題を修正
- markdown-it の HTML サニタイザにあった XSS バイパスを DOMPurify で解消
- postMessage ブリッジをゲートし CSP から unsafe-inline を削除（セキュリティ強化）
- 大きなファイル・ディレクトリ表示時の一時的なフリーズを解消（ディレクトリ列挙・画像埋め込み・エンコーディング判定・mermaid読み込みをメインスレッド外／遅延化）

## v1.7.1

### 修正
- 大きめの mermaid 図で最大文字数エラーになる問題を修正
- CSV ファイルの正規フィールドが誤ったクォート解除処理で破壊される問題を修正
- 対のないクォートを含む CSV でチャンクサイズが異常化する問題を修正
- 特定条件でクラッシュする問題を修正（文字境界外アクセス）
- チャンク強制分割で UTF-8 境界・クォート状態が壊れ文字化けする問題を修正
- 巨大ファイルでエラー表示の種別が誤って丸められる問題を修正
- エンコーディング判定で NUL バイトを誤判定する問題を修正
- 読込エラー後に再描画してもエラーバナーのボタンが無効なままになる問題を修正
- 読込エラー時に表示される幻の空行と、完全読込の誤表示を修正
- チャンク境界の末尾改行が失われ行が連結される問題を修正
- 画像ズーム中にウィンドウをリサイズするとズームが打ち消される問題を修正
- 同一内容のままリネーム/別ファイルを開いた際にファイル種別表示が更新されない問題を修正
- 別ファイル切替直後に表示が不整合な中間状態になる問題を修正
- ラスター画像の初期表示をウィンドウにフィットするよう修正
- 検索バー起動時に全体を構築してフリーズする問題を修正し、表示範囲内検索に変更
- 巨大 SJIS CSV の読込を高速化し、読込中インジケータを追加
- 8KB 超のプレフィックスでエンコーディング判定に失敗した場合に全データで再判定するよう修正

## v1.7.0

### 新機能
- 大容量テキストファイルの行チャンク段階読み込みを実装する (#191)
- Sparkle 2 に移行し自動アップデートを刷新する (#185)

### 修正
- 非 UTF-8 エンコーディング(Shift_JIS / EUC-JP)のフォールバック復号を追加する (#191)

## v1.6.0

### 新機能
- 戻る・進むボタンと行番号トグルをツールバーへ統合する (#181)
- 本文テキスト中のパス文字列をリンク化しクリック動作を統一する (#179)
- リンククリック挙動を整理し HTML 直接プレビューのリンク対応を追加する (#177)
- スクロール位置を UserDefaults に永続化する (#172)
- ソース表示トグルボタンをプレビュー/ソース切替のセグメントコントロールに変更する (#169)

### 修正
- セキュリティレビュー指摘事項を修正する（アップデートフローのコード署名検証、XSS 緩和ほか）(#178)
- キーボードスクロールを Safari に合わせて整理する (#176)
- 検索窓のスクロール奪取・トグル配置・表示の使い勝手を改善する (#175)
- フォルダー選択後にアプリ切替すると選択が消える問題を修正する (#167)
- プレビューのスクロール位置・表示モードが復元されない問題を修正する (#166)

## v1.5.0

### 新機能
- プレビュー内検索(Cmd+F)を追加する (#163)
- プレビューのスクロールをページ/半ページ/行単位に整理する (#158)

### 修正
- サイドバー再表示手段を左端ハンドルからツールバーボタンに統一する (#160)
- サイドバー表示切替のショートカットを Cmd+B から Cmd+S に変更する (#156)
