# Changelog

stable リリースのユーザー影響のある変更を記録する。
dev リリースの全変更履歴は [GitHub Releases](https://github.com/YTommy109/befold/releases) を参照。
v1.5.0 より前の履歴は GitHub Releases の各タグページを参照。

## v1.13.1

### 新機能
- Help メニューから GitHub Issues を開けるようにしました。

### 不具合修正
- プレビューにフォーカスがあるときフォルダー名が薄く表示される問題を修正しました。

## v1.13.0

### 新機能
- サイドバーに git ステータス表示を追加しました（これまで開発ビルド限定）。
- ソース表示で git 差分を表示できるようにしました（これまで開発ビルド限定）。
- サイドバーのツリー展開表示を追加しました（これまで開発ビルド限定）。
- サイドバーの並び順を永続化するようにしました。
- サイドバーヘッダーのコントロールを整理し、パスポップアップを導入しました。
- CSV/TSV のセル内エスケープシーケンスをテーブル表示で展開するようにしました。

### 修正
- ツリー表示の開閉三角をクリックで開閉できるようにしました。
- 提示対象（ファイル/フォルダー一覧）に追随しない不具合を修正しました。
- 文書内アンカーリンクとグローバル gitignore の不具合を修正しました。
- サイドバーのフォルダー移動・展開判定と描画要求・リポジトリ記録の不具合を修正しました。
- サイドバーの選択とフォルダー移動が食い違う不具合を修正しました。

## v1.12.3

### 修正
- 同一ファイルを複数ウィンドウで開いたときに表示モードが揃わない問題を修正しました。
- ファイルをリネームした際に、未保存の表示モード（CLI の `--source` / `--preview` 指定）が破棄される問題を修正しました。
- コード種別のファイルで選択済みの表示を選び直すとスクロール位置を失う問題を修正しました。
- 表示を往復で切り替えた際に、ズーム・スクロール位置が古い保存値に巻き戻ることがある問題を修正しました。
- ファイルをリネームするとスクロール位置の保存先がずれて位置が巻き戻る問題を修正しました。
- 絞り込みで選択行が隠れても矢印キーで移動できるようにしました。
- 名前フィルターの結果が 0 件のときと、そもそも空のフォルダーとを区別して表示するようにしました。
- フォルダーの読み取りに失敗したときに「空」と区別できるようにしました（ルート一覧・プレビュー・Quick Open）。
- サイドバーの表示に入っていた 2 件の不具合を修正しました。
- 存在しないファイルのブックマークを取り除けるようにしました。
- セッション復元時に、同じパスが重複しているとウィンドウが別グループから奪われる問題を修正しました。
- 他のウィンドウが同じファイルを表示している間は、セッション記録から消さないようにしました。
- 参照解決の途中でウィンドウを閉じたときに起きる 2 件の不具合を修正しました。
- ページを読み直したあとに古い参照解決の応答が反映される問題を修正しました。
- Markdown のハイライト表示で、パス参照が途中で分割されると参照リンクにならない問題を修正しました。
- 大きなファイルの追記時に、切り詰めバナーが誤ったタイミングで表示される問題を修正しました。
- QuickLook プレビューで Space キーの抑止が効かない問題を修正しました。
- git 連携の内部処理に含まれていたデータ競合を解消し、安定性を高めました。

## v1.12.2

### 新機能
- サイドバーのコンテキストメニューに「新しいタブで開く」を追加しました。

### 修正
- 既に開いているファイルを「新しいウィンドウで開く」で開いたときに、操作が無視される問題を修正しました。

## v1.12.0

### 新機能
- サイドバーの Cmd+↑ で上位フォルダーへ移動できるようにしました。
- フォルダー階層を降りたとき一覧の先頭が選択された状態になるようにしました。
- 最近使った項目とブックマークのメニューにパスを併記するようにしました。
- フォルダーを再訪したとき直前の選択を復元するようにしました。

### 修正
- サイドバーの選択移動で選択行までスクロールしない問題を修正しました。
- フォルダー移動時にプレビューがサイドバーの選択に追従しない問題を修正しました。
- About ダイアログのテキストを選択・コピーできるようにしました。
- 開いている文書の URL を保持先で native 裏打ちに揃えるようにしました。
- previewTarget の導出をディスク状態に依存しない処理に改善しました。
- ソート済みの一覧を連結後に並べ直さないようにしました。
- メニュー検証と列挙で払っていた無駄なコストを削減しました。

## v1.11.6

### 修正
- ファイルに NUL バイトが混入していると、内容が文字化けして表示される問題を修正

## v1.11.4

### 新機能
- AI 連携画面に skill ファイルのコピーボタンを追加しました。
- OSS 謝辞を Markdown としてレンダリング表示するようにしました。

## v1.11.1

### 新機能
- サイドバーにファイル名フィルター機能を追加しました。
- サイドバーヘッダーに基準ディレクトリを表示するようにしました。

### 修正
- シンタックスハイライトの span 境界をまたぐ検索がヒットしない問題を修正しました。

## v1.11.0

### 新機能
- コードフォント設定機能を追加しました。

### 修正
- コードフォントサイズをアクセシビリティ文字サイズに追従させるようにしました。

## v1.10.1

### 新機能
- サイドバー幅を広げ、ファイル名にホバーすると全文をツールチップ表示するようにしました。

### 修正
- マイグレーション検査の失敗理由が分かるようにしました。

## v1.10.0

### 新機能
- Cmd+P で Quick Open パネルを開き、ファイル名や候補を絞り込んで開けるようにしました。
- Quick Open の候補で入力に一致した文字をハイライト表示するようにしました。
- コードブロック表示にインデントガイド(縦線)を追加しました。
- 相対パスコピーを git リポジトリ内では git root 基準で行うようにしました。

### 修正
- charset 未宣言の .htm/.html ファイルを直接ロードした際の文字化けを修正しました。
- Quick Open 表示中に他アプリへ切り替えるとパネルが残ってしまう問題を修正しました。
- サイドバー切替時に裏のウィンドウが前面化してフォーカスを奪う問題を修正しました。
- Quick Open パネルが水平中央からずれる問題を修正しました。
- CLI の `--version` 表示をビルド番号付き(GUI 表記)に統一しました。

## v1.9.0

### 修正
- 大きい Markdown ファイルを開いたときにメモリ使用量が極端に増える問題を修正しました。これまで Markdown は全体を一度に描画していたため、9MB のファイルで 3GB 近くのメモリを消費していましたが、段階的に描画するようになり 70MB 程度に収まります。
- これに伴い、開ける Markdown ファイルのサイズ上限が 10MB から 100MB に広がりました。

## v1.8.0

### 新機能
- QuickLook 拡張を追加しました。Finder でファイルを選んでスペースキーを押すだけで、befold を起動せずに Mermaid / Markdown のプレビューを表示できます。対象拡張子は `.mmd` / `.mermaid` / `.md` / `.markdown` などで、プレビューは描画完了を待ってから表示されるため途中の状態が見えることはありません。
- 大きいファイルや行指向でないファイルでも、QuickLook のプレビューが空白のまま止まらないようにしました。

## v1.7.3

### 新機能
- ドキュメント内のパス参照を表示時に解決し、実在するファイルだけをリンクとして表示するようになりました。相対パスで見つからない場合は git 追跡ファイルへフォールバックし、サフィックス一致と近さでもっとも妥当な候補を選びます。

### 修正
- パス参照のリンク化に関する不具合を修正しました（エスケープされた `#` をファイル名の一部として扱う、存在しないファイルをリンク化してしまうケースの排除、git 追跡ファイル一致時の実在確認）。
- git の一時的な失敗が解決結果としてキャッシュに固定されないようにしました。
- git 実行をリポジトリ設定から切り離し（絶対パス・固定環境）、設定由来で任意のコマンドが実行されうる問題を塞ぎました。
- git 実行を打ち切った際にプロセスやスレッド、ファイルディスクリプタが残る問題を修正しました。
- CLI から追加したブックマークがツールバーへ即座に反映されるようにしました。
- CLI と GUI で同時にブックマークを更新した際に書き込みが失われる問題を修正しました。
- CLI の転送完了待ちを非同期化し、転送の成否を正しく判定するようにしました。
- CLI からのアプリ起動に失敗した場合、stderr に診断メッセージを出力するようにしました。

### パフォーマンス
- パス参照の解決をメインスレッド外で実行し、候補索引をバッチ全体で使い回すようにして、表示時の待ち時間を短縮しました。
- git 追跡ファイルの照合索引をキャッシュし、バッチごとの再構築をなくしました。

## v1.7.2

### 修正
- HTML 表示直後の切替で空白表示になる問題を修正
- 完全一致・末尾改行なしのファイルで偽のトランケーション表示が出る問題を修正
- markdown-it の HTML サニタイザにあった XSS バイパスを DOMPurify で解消
- postMessage ブリッジをゲートし CSP から unsafe-inline を削除（セキュリティ強化）
- 大きなファイル・ディレクトリ表示時の一時的なフリーズを解消（ディレクトリ列挙・画像埋め込み・エンコーディング判定・mermaid読み込みをメインスレッド外／遅延化）

## v1.7.1

### 修正
- 大きめの mermaid 図で最大文字数エラーになる問題を修正
- CSV ファイルの正規フィールドが誤ったクォート解除処理で破壊される問題を修正
- 対のないクォートを含む CSV でチャンクサイズが異常化する問題を修正
- 特定条件でクラッシュする問題を修正（文字境界外アクセス）
- チャンク強制分割で UTF-8 境界・クォート状態が壊れ文字化けする問題を修正
- 巨大ファイルでエラー表示の種別が誤って丸められる問題を修正
- エンコーディング判定で NUL バイトを誤判定する問題を修正
- 読込エラー後に再描画してもエラーバナーのボタンが無効なままになる問題を修正
- 読込エラー時に表示される幻の空行と、完全読込の誤表示を修正
- チャンク境界の末尾改行が失われ行が連結される問題を修正
- 画像ズーム中にウィンドウをリサイズするとズームが打ち消される問題を修正
- 同一内容のままリネーム/別ファイルを開いた際にファイル種別表示が更新されない問題を修正
- 別ファイル切替直後に表示が不整合な中間状態になる問題を修正
- ラスター画像の初期表示をウィンドウにフィットするよう修正
- 検索バー起動時に全体を構築してフリーズする問題を修正し、表示範囲内検索に変更
- 巨大 SJIS CSV の読込を高速化し、読込中インジケータを追加
- 8KB 超のプレフィックスでエンコーディング判定に失敗した場合に全データで再判定するよう修正

## v1.7.0

### 新機能
- 大容量テキストファイルの行チャンク段階読み込みを実装する (#191)
- Sparkle 2 に移行し自動アップデートを刷新する (#185)

### 修正
- 非 UTF-8 エンコーディング(Shift_JIS / EUC-JP)のフォールバック復号を追加する (#191)

## v1.6.0

### 新機能
- 戻る・進むボタンと行番号トグルをツールバーへ統合する (#181)
- 本文テキスト中のパス文字列をリンク化しクリック動作を統一する (#179)
- リンククリック挙動を整理し HTML 直接プレビューのリンク対応を追加する (#177)
- スクロール位置を UserDefaults に永続化する (#172)
- ソース表示トグルボタンをプレビュー/ソース切替のセグメントコントロールに変更する (#169)

### 修正
- セキュリティレビュー指摘事項を修正する（アップデートフローのコード署名検証、XSS 緩和ほか）(#178)
- キーボードスクロールを Safari に合わせて整理する (#176)
- 検索窓のスクロール奪取・トグル配置・表示の使い勝手を改善する (#175)
- フォルダー選択後にアプリ切替すると選択が消える問題を修正する (#167)
- プレビューのスクロール位置・表示モードが復元されない問題を修正する (#166)

## v1.5.0

### 新機能
- プレビュー内検索(Cmd+F)を追加する (#163)
- プレビューのスクロールをページ/半ページ/行単位に整理する (#158)

### 修正
- サイドバー再表示手段を左端ハンドルからツールバーボタンに統一する (#160)
- サイドバー表示切替のショートカットを Cmd+B から Cmd+S に変更する (#156)
