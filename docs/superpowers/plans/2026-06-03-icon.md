# mmdview macOS アイコン 実装プラン

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Mermaid フローチャートモチーフの macOS ネイティブアプリアイコン（`.icns`）を生成し、PyInstaller ビルドに組み込む。

**Architecture:** SVG マスターファイル（`static/icons/icon.svg`）を作成し、`cairosvg` で各サイズの PNG を生成、macOS 標準の `iconutil` で `.icns` に変換する。変換処理は `scripts/build_icon.py` にまとめ、`pyproject.toml` の `icon` タスクから呼び出す。最後に `mmdview.spec` の `icon=None` を差し替えてビルドに反映する。

**Tech Stack:** SVG（手書き）、cairosvg（PNG変換）、macOS iconutil（.icns生成）、taskipy、PyInstaller

---

### Task 1: マスター SVG の作成

**Files:**
- Create: `static/icons/icon.svg`

- [ ] **Step 1: ディレクトリを作成する**

```bash
mkdir -p static/icons
```

- [ ] **Step 2: `static/icons/icon.svg` を作成する**

```xml
<svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 512 512" width="512" height="512">
  <defs>
    <linearGradient id="bg" x1="0%" y1="0%" x2="100%" y2="100%">
      <stop offset="0%" stop-color="#1d4ed8"/>
      <stop offset="45%" stop-color="#7c3aed"/>
      <stop offset="100%" stop-color="#0891b2"/>
    </linearGradient>
    <linearGradient id="node" x1="0%" y1="0%" x2="100%" y2="100%">
      <stop offset="0%" stop-color="#f0abfc"/>
      <stop offset="100%" stop-color="#a5f3fc"/>
    </linearGradient>
  </defs>
  <!-- Background -->
  <rect x="0" y="0" width="512" height="512" rx="112" fill="url(#bg)"/>
  <!-- Subtle top highlight -->
  <rect x="0" y="0" width="512" height="256" rx="112" fill="white" opacity="0.04"/>
  <!-- Edge 1: top node → middle node (line + arrowhead) -->
  <line x1="256" y1="192" x2="256" y2="220" stroke="white" stroke-width="8" stroke-linecap="round" opacity="0.8"/>
  <polygon points="242,220 270,220 256,248" fill="white" opacity="0.85"/>
  <!-- Edge 2: middle node → bottom node (line + arrowhead) -->
  <line x1="256" y1="332" x2="256" y2="360" stroke="white" stroke-width="8" stroke-linecap="round" opacity="0.8"/>
  <polygon points="242,360 270,360 256,388" fill="white" opacity="0.85"/>
  <!-- Top node -->
  <rect x="148" y="108" width="216" height="82" rx="22" fill="url(#node)"/>
  <!-- Middle node -->
  <rect x="148" y="248" width="216" height="82" rx="22" fill="url(#node)" opacity="0.88"/>
  <!-- Bottom node -->
  <rect x="148" y="388" width="216" height="82" rx="22" fill="url(#node)" opacity="0.76"/>
</svg>
```

- [ ] **Step 3: SVG を目視確認する**

```bash
open static/icons/icon.svg
```

Preview.app でデザイン（縦フロー3ノード、ビビッドカラー）が表示されること。

- [ ] **Step 4: コミットする**

```bash
git add static/icons/icon.svg
git commit -m "feat: add master SVG icon"
```

---

### Task 2: アイコン変換スクリプトの作成

**Files:**
- Create: `scripts/build_icon.py`
- Modify: `pyproject.toml`（`icon` タスク追加）
- Modify: `.gitignore`（生成物を除外）

- [ ] **Step 1: `cairosvg` を開発依存として追加する**

```bash
uv add --dev cairosvg
```

Expected: `uv.lock` が更新され `cairosvg` が追加されること。

- [ ] **Step 2: `scripts/` ディレクトリを作成する**

```bash
mkdir -p scripts
```

- [ ] **Step 3: `scripts/build_icon.py` を作成する**

```python
#!/usr/bin/env python3
"""SVG → PNG → .icns 変換スクリプト（macOS 専用）"""
import shutil
import subprocess
import sys
from pathlib import Path

import cairosvg

ROOT = Path(__file__).parent.parent
SVG = ROOT / "static/icons/icon.svg"
ICONSET = ROOT / "static/icons/icon.iconset"
ICNS = ROOT / "static/icons/icon.icns"

# (通常解像度サイズ, @2x として使われるサイズ) のペア
# iconutil が要求するファイル名の一覧:
#   icon_16x16.png, icon_16x16@2x.png (=32px),
#   icon_32x32.png, icon_32x32@2x.png (=64px),
#   icon_128x128.png, icon_128x128@2x.png (=256px),
#   icon_256x256.png, icon_256x256@2x.png (=512px),
#   icon_512x512.png, icon_512x512@2x.png (=1024px)
ICONSET_SIZES = [
    (16, "icon_16x16.png"),
    (32, "icon_16x16@2x.png"),
    (32, "icon_32x32.png"),
    (64, "icon_32x32@2x.png"),
    (128, "icon_128x128.png"),
    (256, "icon_128x128@2x.png"),
    (256, "icon_256x256.png"),
    (512, "icon_256x256@2x.png"),
    (512, "icon_512x512.png"),
    (1024, "icon_512x512@2x.png"),
]


def build() -> None:
    if ICONSET.exists():
        shutil.rmtree(ICONSET)
    ICONSET.mkdir(parents=True)

    svg_data = SVG.read_bytes()

    for size, filename in ICONSET_SIZES:
        png = cairosvg.svg2png(bytestring=svg_data, output_width=size, output_height=size)
        (ICONSET / filename).write_bytes(png)

    result = subprocess.run(
        ["iconutil", "-c", "icns", str(ICONSET), "-o", str(ICNS)],
        capture_output=True,
        text=True,
    )
    if result.returncode != 0:
        print(result.stderr, file=sys.stderr)
        sys.exit(1)

    shutil.rmtree(ICONSET)
    print(f"Created: {ICNS}")


if __name__ == "__main__":
    build()
```

- [ ] **Step 4: スクリプトを実行して `.icns` が生成されることを確認する**

```bash
uv run python scripts/build_icon.py
```

Expected output:
```
Created: /…/static/icons/icon.icns
```

生成されたファイルを確認:
```bash
ls -lh static/icons/icon.icns
open static/icons/icon.icns
```

Preview.app で各サイズのアイコンが表示されること。

- [ ] **Step 5: `.icns` と `.iconset` を `.gitignore` に追加する**

`.gitignore` の末尾に追記:

```
static/icons/*.icns
static/icons/*.iconset
```

- [ ] **Step 6: `pyproject.toml` に `icon` タスクを追加する**

`[tool.taskipy.tasks]` セクションに追加:

```toml
icon       = "python scripts/build_icon.py"
```

追加後の該当セクション:

```toml
[tool.taskipy.tasks]
dev        = "python -m backend.app"
server     = "uvicorn backend.main:app --reload --port 8000"
test       = "pytest -v --cov --cov-report=term-missing"
build      = "pyinstaller mmdview.spec"
icon       = "python scripts/build_icon.py"
lint       = "ruff check ."
format     = "ruff format ."
typecheck  = "ty check"
"lint:md"  = "markdownlint-cli2 '*.md'"
```

- [ ] **Step 7: タスク経由で動作確認する**

```bash
uv run task icon
```

Expected: `Created: .../static/icons/icon.icns`

- [ ] **Step 8: コミットする**

```bash
git add scripts/build_icon.py pyproject.toml uv.lock .gitignore
git commit -m "feat: add icon build script and task (SVG → PNG → .icns)"
```

---

### Task 3: PyInstaller spec にアイコンを設定してビルド確認

**Files:**
- Modify: `mmdview.spec:64`

- [ ] **Step 1: `.icns` を事前生成する**

```bash
uv run task icon
ls static/icons/icon.icns
```

- [ ] **Step 2: `mmdview.spec` の `icon=None` を変更する**

`mmdview.spec:64` を以下に変更:

```python
# 変更前
icon=None,

# 変更後
icon='static/icons/icon.icns',
```

- [ ] **Step 3: アプリをビルドする**

```bash
uv run task build
```

Expected: `dist/mmdview.app` が生成されること。

- [ ] **Step 4: アイコンが適用されていることを確認する**

```bash
open dist/
```

Finder で `dist/mmdview.app` のアイコンがカスタムデザインになっていること。

```bash
open dist/mmdview.app
```

アプリが正常に起動すること。

- [ ] **Step 5: コミットする**

```bash
git add mmdview.spec
git commit -m "feat: set custom .icns icon in PyInstaller spec"
```
