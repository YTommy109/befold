# 型グループ（Foo.swift + 同ディレクトリの Foo+*.swift）の行数閾値に対する恒久例外。
#
# scripts/check-type-group-size.sh は「型グループの行数が閾値（400 行）以下であること」
# だけを見る。ここに列挙したグループはその代わりに個別の上限で判定する。
#
# 形式: <グループキー><TAB><上限行数><TAB><理由>
#
# 理由は必須。空のエントリはスクリプトが弾く（「なぜ 400 に収まらないか」を書かないと
# 登録できない形にすることで、例外の追加を差分レビューで必ず目に付くようにする）。
# 例外は現状値の凍結（ラチェット）ではない。上限を超えない限り増減は自由で、逆に
# 上限まで太らせてよいという意味でもない。返済して閾値以下へ戻せたらこの行を消す。
BefoldApp/befold/App/ViewerWindowController	931	NSResponder チェーンの都合で移動できない @objc メニューアクションと validate 対応表、および init の Parameter doc（約 120 行）が支配的で、責務分離を尽くしても 400 行へは到達しない（TASK-441 / TASK-453 で 1255 → 802 行まで返済済み。判断の経緯は TASK-441 の AC #1）。TASK-593 の 3 サブタスクで 905 → 931 行（593.1 の撤去で 909 → 905、593.2 の窓種別で 922、593.3 のスライドキー配線で 931）。増えたのはいずれも窓の生成・後始末の配線で、判定と純粋な表は ViewerWindowKind / SidebarInheritance / SlideKeyAction / FileListSnapshot 側へ置いてある。上限は常に実測値へ張り付ける
BefoldApp/befold/App/SidebarNavigator	425	実処理は既に 8 つの協力型へ出してあり、本体に残るのは協力型どうしを繋ぐハブ（1 行委譲 20 本）と doc。唯一の切り出し候補だった navigateToFolder は、協力型が意図して private（TASK-319 / TASK-442.5）なため移した先もこの型の薄い委譲を通り、行数が移るだけで結合が下がらない。moveCurrentDirectory は currentDirectory を書き換える唯一の経路（TASK-465）で型外 4 箇所から呼ばれるため動かせず、awaitSettled と pending*Task は 3 つの private 協力型を跨ぐため出すと private の理由と衝突する。関心の同居を測る指標は飽和していない（protocol 準拠 0・注入クロージャ 3 = 上限ちょうど・@objc 0）。判断の全文は型 doc にあり、それを書いたぶん 398 → 425 になった（TASK-604.7）。上限は常に実測値へ張り付ける
BefoldApp/befold/App/MainMenuBuilder	449	メニュー項目の定義そのもので、責務で切っても分かれない（TASK-585 / TASK-547 で 2 回実測し、同じ結論に達した）。@MainActor enum の名前空間で stored property・protocol 準拠・注入クロージャがいずれも 0 個のため、関心の同居を測る指標が存在しない。Foo+Bar.swift へ割っても合算は 1 行も減らず、別の型名へ出すと配布サイトのショートカット検証が読む MainMenuBuilder*.swift の glob（site/vitest.config.ts と .github/workflows/site.yml の paths）から外れる。TASK-535 で Bookmarks をトップレベルへ独立させ 399 → 436 行（+BookmarksMenu.swift の新設 38 行と、固定部トグルを 2 経路から呼ぶ理由の doc）。TASK-536.1 で固定部に「ブックマークを編集…」を足して 449 行。この扱いは docs/dev/rules/product-code.md の責務分離節が規定している。上限は常に実測値へ張り付ける
