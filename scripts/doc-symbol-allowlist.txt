# scripts/check-doc-symbols.sh の除外リスト。
# 規約文書が `Type.member` 形式で名指しするが、リポジトリ内に宣言が無くてよいものを
# 1 行 1 件で列挙する（文書に書いてあるとおりの綴りで完全一致）。
#
# 除外してよいのは次のどちらか:
#   - 例示のための架空名（「こういう名前を付ける」という説明の中の仮の名前）
#   - 外部フレームワーク / 外部ツールの API（AppKit・Sparkle・SwiftLint など）
#
# 実在しなくなった自前のシンボルをここへ足して黙らせないこと。それは TASK-374 の
# 陳腐化そのもので、直すべきは文書側の記述。
#
# （現時点で除外が必要な引用はない。ファイル名 `Foo.swift` 形式はスクリプト側で除外する。）
